import { JSX } from "preact";
import { ReactNode } from "../../../../../modules/common/React";
import { Rect } from "../../../../../modules/common/Rect";
import { UploadedFile } from "upload-js";
import "./ImageEditorLayout.scss";
interface Props {
    actions: ReactNode;
    header?: ReactNode;
    image: (props: {
        imageUrl: string;
        imgDimensions: Rect;
    }) => ReactNode;
    originalImage: UploadedFile;
}
export declare const ImageEditorLayout: ({ actions, originalImage, header, image }: Props) => JSX.Element;
export {};
