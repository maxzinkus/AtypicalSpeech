import { JSX } from "preact";
import { ReactNode } from "../../modules/common/React";
interface Props<T> {
    children?: ReactNode;
    className?: string;
    onMove: (xDelta: number, yDelta: number, start: T) => void;
    startingValue: T;
    style?: JSX.CSSProperties;
}
export declare const Draggable: <T extends unknown>({ children, className, onMove: onMoveCallback, style, startingValue }: Props<T>) => JSX.Element;
export {};
